version: 2

models:
  - name: stg_scored_transactions
    columns:
      - name: transaction_id
        tests: [unique, not_null]
  - name: fact_transactions
    columns:
      - name: transaction_id
        tests: [unique, not_null]
      - name: amount
        tests:
          - not_null
