{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] } },
      "targets": [
        { "expr": "fraud_transactions_total", "legendFormat": "Total Transactions", "refId": "A" }
      ],
      "title": "Total Scored Transactions",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short", "color": { "mode": "palette-classic" } }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [
        { "expr": "fraud_declined_total", "legendFormat": "Declined", "refId": "A" }
      ],
      "title": "Declined Transactions",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 8 },
      "id": 3,
      "targets": [
        { "expr": "pg_stat_database_numbackends{datname=\"payment_scoring\"}", "legendFormat": "DB Connections", "refId": "A" }
      ],
      "title": "PostgreSQL Connections",
      "type": "timeseries"
    }
  ],
  "refresh": "10s",
  "schemaVersion": 39,
  "tags": ["fraud", "data-engineering"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "title": "Fraud Platform Overview",
  "uid": "fraud-platform-overview",
  "version": 1
}
